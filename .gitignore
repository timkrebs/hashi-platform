# Secrets / local environment — never commit
.env
*.tfvars
!*.tfvars.example

# Terraform working files
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
